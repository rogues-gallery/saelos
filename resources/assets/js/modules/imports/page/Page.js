import React from "react";
import PropTypes from "prop-types";
import Record from "./panels/record";

const Page = props => (
  <React.Fragment>
    <Record />
  </React.Fragment>
);

Page.propTypes = {};

export default Page;
